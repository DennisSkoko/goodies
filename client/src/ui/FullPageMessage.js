import styled from 'styled-components'

const FullPageMessage = styled.section`
  margin: ${({ theme }) => theme.spacing.xl} auto 0;
  padding: ${({ theme }) => theme.spacing.sm};
  max-width: 500px;
`

export default FullPageMessage
